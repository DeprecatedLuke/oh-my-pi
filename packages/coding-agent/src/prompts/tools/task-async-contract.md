No polling or waiting on jobs. Results auto-deliver; `hub jobs` is an intervention snapshot that acknowledges delivery, suppressing duplicate `async-result`. Job IDs live in process memory roughly five minutes; afterward, reach by agent ID via `hub send`, `agent://<id>`, or `history://<id>`. `completed` means the subagent yielded, not that artifacts were verified.
